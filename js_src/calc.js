module.exports = {

    findSign: function(date) {
            return "hi";
        //find sun sign using date
        //returns enum Signs
    },
    //same as above, but more specified
    findSunSign: function(date) {
            return findSign(date);
    }
        //find sun sign using date
        //returns enum Signs

}
